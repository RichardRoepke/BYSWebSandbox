<% provide(:title, "Edit User") %>

<div class="form">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for @user, url: {action: "update"} do |f| %>
      <%= f.text_field :email %>
      <%= f.submit "Update" %>
    <% end %>
  </div>
</div>